'use strict';

describe('loanApp.loan module', function() {

  beforeEach(module('loanApp.loan'));

  describe('loan controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('loan');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
